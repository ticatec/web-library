{
  "name": "@ticatec/app-data-manager",
  "version": "0.1.7",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "tsc && cp ./package.json ./dist/",
    "publish:public": "cd ./dist && npm publish --access public"
  },
  "devDependencies": {
    "@ticatec/enhanced-utils": "^0.0.4"
  }
}
