{
  "name": "@ticatec/enhanced-utils",
  "version": "1.0.0",
  "type": "commonjs",
  "main": "index.js",
  "types": "index.d.ts",
  "description": "This is a utility library to enhance the Object and Array, it also provide some basic functions such as: isString, isArray, isEmpty etc.",
  "scripts": {
    "test": "mocha --require ts-node/register test/*-test.ts",
    "clean": "rm -rf ./dist",
    "build": "tsc && cp ./package.json ./dist/ && cp *.md ./dist/",
    "publish:public": "cd ./dist && npm publish --access public"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1"
  }
}
