{
  "name": "@ticatec/dictionary",
  "version": "0.9.2",
  "description": "This package contains a Dictionary Manager, which is responsible for managing a collection of data dictionaries. The manager allows for the registration, initialization, and refresh of data dictionaries.",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "keywords": ["dictionary", "tree", "map", "list", "array", "web", "data", "manager"],
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "tsc && cp ./package.json ./dist/ && cp *.md ./dist/",
    "publish:public": "cd ./dist && npm publish --access public"
  },
  "license": "MIT",
  "dependencies": {
    "@ticatec/enhanced-utils": "^1.0.1"
  }
}
